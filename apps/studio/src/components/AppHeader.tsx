/**
 * Retired — the top bar is now part of the AppShell organism
 * (`@/components/atomic/organisms/AppShell`), which pages render around
 * their content. Kept as a no-op so App.tsx keeps compiling until every
 * page has moved to AppShell.
 */
export default function AppHeader(): null {
	return null;
}
