{
	"name": "@freedi/rules-tests",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Firestore security-rules tests. Isolated from the app dependency tree on purpose: @firebase/rules-unit-testing peers on firebase ^12 while the apps are on ^11.",
	"scripts": {
		"test": "node --test"
	},
	"devDependencies": {
		"@firebase/rules-unit-testing": "^5.0.1",
		"firebase": "^12.0.0"
	}
}
