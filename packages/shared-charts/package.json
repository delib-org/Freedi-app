{
  "name": "@freedi/shared-charts",
  "version": "1.0.0",
  "private": true,
  "description": "Framework-agnostic SVG chart geometry plus the modular indicator registry behind the Agora and Studio dashboards",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./styles/*": "./styles/*"
  },
  "dependencies": {
    "@freedi/shared-types": "*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
